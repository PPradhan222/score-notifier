<%= form_with(model: player_runs_notifier, local: true) do |form| %>
  <% if player_runs_notifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_runs_notifier.errors.count, "error") %> prohibited this player_runs_notifier from being saved:</h2>

      <ul>
        <% player_runs_notifier.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :team_squad_member_id %>
    <%= form.text_field :team_squad_member_id %>
  </div>

  <div class="field">
    <%= form.label :runs %>
    <%= form.number_field :runs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
